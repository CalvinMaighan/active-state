# React kanban

One file: [`src/main.tsx`](./src/main.tsx) (+ [`styles.css`](./styles.css)).

`key()` + `<ActiveState />` + `useActiveState` — same board UX as the HTML example (persist, ghost drag, collapsible columns).

```bash
bun run examples:react
```

http://localhost:5179
