# Astro kanban

One file: [`src/pages/index.astro`](./src/pages/index.astro) (+ synced `styles.css` / `active-state.min.js`).

Same declarative board as HTML; `predev` copies assets from the package build + `examples/html/styles.css`.

```bash
bun run examples:astro
```

http://localhost:5180
