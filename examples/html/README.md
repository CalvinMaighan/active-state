# HTML kanban

One file: [`index.html`](./index.html) (+ [`styles.css`](./styles.css)).

Declarative `active-*` board: drag ghost, empty slots, collapsible `<details>` columns, `persist: true`.

```bash
bun run examples:html
```

http://localhost:5177
