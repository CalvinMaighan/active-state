# HTMX kanban

One file: [`index.html`](./index.html) (+ [`styles.css`](./styles.css)).

Same declarative board as HTML, plus HTMX `hx-get="/api/suggest"`.

```bash
bun run examples:htmx
```

http://localhost:5178
